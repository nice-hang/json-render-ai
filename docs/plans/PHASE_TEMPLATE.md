# 阶段 N：<名称>

- 状态：`draft`
- 权威规格：`<相对链接>`
- 依赖：`<前置阶段、契约或 N/A>`
- 目标结果：`<一句可观察描述>`

## 目标

描述本阶段要证明的唯一用户可见或开发者可见结果。

## 范围

- <包含的行为>

## 非目标

- <明确排除的工作及原因>

## 入口门

- [ ] 验收标准已经确认。
- [ ] 依赖和外部契约已经可用。
- [ ] 本阶段最高风险假设已有验证计划。
- [ ] 已列出预计修改和明确不修改的区域。
- [ ] 已记录工作树基线。

## 架构与约束

- 状态所有者：`<所有者>`
- 读写路径：`<路径>`
- 公开契约：`<契约>`
- 副作用所有者：`<所有者>`
- 安全边界：`<边界>`
- 刻意简化：`<简化方案和升级条件>`

## 执行门

| 门禁 | 要求         | 状态   | 证据 |
| ---- | ------------ | ------ | ---- |
| G1   | <阻塞性要求> | 待确认 |      |

未满足执行门时，可以进行隔离测试或原型验证，但不能接入用户可见入口，也不能声称完成。

## 任务

### 任务 1：<范围明确的标题>

**验收标准：** `AC-...`

**文件：**

- 新增：`<路径>`
- 修改：`<路径>`
- 禁止修改：`<路径或区域>`

**步骤：**

- [ ] 增加或识别失败的验证切口。
- [ ] 实现最小完整行为。
- [ ] 运行聚焦验证。
- [ ] 检查范围内差异。
- [ ] 记录证据。

**验证：**

```bash
<聚焦验证命令>
```

**预期结果：** `<具体可观察结果>`

## 退出门

- [ ] 每条关联验收标准都有证据。
- [ ] 主流程通过。
- [ ] 至少一个有意义的失败流程通过。
- [ ] 按要求运行类型检查、代码检查、测试和构建。
- [ ] 按要求运行人工或浏览器验证。
- [ ] 最终差异仅包含已确认范围。
- [ ] 已更新 `docs/STATE.md`。
- [ ] 用户已确认阶段交接。

## 证据

- `<docs/evidence/ 下的相对链接>`

## 回滚或降级

描述本阶段无法交付时可保留的安全可用状态。

## 最终交接

- 已交付：
- 遗留风险：
- 延后事项：
- 下一道门：
- 下一动作：
