# WebMCP Challenge Video Narration

Target: 2 minutes 20 seconds at a calm pace. Record the application at 1280×720 or larger, use original English narration, and do not add music. Follow [`DEMO_SCRIPT.md`](DEMO_SCRIPT.md) for the exact interactions.

The narration-only input used by the local video builder is [`VIDEO_NARRATION.txt`](VIDEO_NARRATION.txt).

## 0:00–0:20 — Shared workspace

“This is json-render-ai, a low-code workspace where a person and a browser agent safely build the same application. The component tree, real json-render canvas, type-driven properties, and shared Activity trail are all visible together. Reset gives us the same Northstar CRM for every run.”

## 0:20–0:40 — Human command

“I will first change this introduction as a human. Saving the property sends a structured update through the Command Runtime. The canvas changes immediately, the revision advances, Undo becomes available, and Activity records the source as human.”

## 0:40–1:25 — Native WebMCP agent commands

“Now the browser agent discovers the page’s native WebMCP surface: exactly eight documented tools, with structured input schemas. It reads the current app and stable component IDs without changing state. Then it adds an Agent forecast metric, updates the value to five hundred and thirty thousand dollars, and moves it to the first position. These are not DOM clicks or a test shim. Each WebMCP tool enters the same validated Command Runtime and the human interface stays synchronized.”

## 1:25–2:05 — Confirmation and recovery

“Destructive actions stay under explicit control. I open deletion from the visible interface, inspect the impact, and cancel. The AppSpec does not change. The agent’s first remove call also performs only a preview and returns a revision-bound confirmation token. A matching second call removes exactly that component. Undo then restores the previous AppSpec, and validation confirms the application remains valid.”

## 2:05–2:20 — Outcome

“The Activity trail now shows both human and agent commands on one shared state. WebMCP gives the agent a reliable product API, while the person keeps visual context, confirmation, auditability, and recovery. That is json-render-ai.”

## Recording acceptance checklist

- [ ] Native Chrome shows `WebMCP registered`; no shim or developer console is used as evidence.
- [ ] The recording visibly includes discovery, read, add, update, move, cancel, confirmed removal, Undo, validation, and human/agent Activity.
- [ ] Final duration is below 3:00 and the narration is clear throughout.
- [ ] No credentials, notifications, personal data, third-party music, or unrelated browser chrome appear.
- [ ] The uploaded YouTube video and its audio work in a logged-out session.
